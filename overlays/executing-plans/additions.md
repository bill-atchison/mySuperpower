
## mySuperpower additions — HTML implementation notes

After completing the plan, capture implementation notes as a **self-contained, styled HTML document** using the template at `skills/executing-plans/templates/implementation-notes-template.html`. Save to `docs/mySuperpower/implementation-notes/YYYY-MM-DD-<feature>.html` and commit. Open it in the browser for a quick acceptance gate before finishing the branch.
