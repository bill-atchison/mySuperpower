{
  "skill": "executing-plans",
  "replaces": [
    {
      "find": "Read the plan once, note its context and Global Constraints, and create a\ntodo per task.",
      "replace": "Read the plan once, note its context and Global Constraints, and create a\ntodo per task. **If PLAN_FILE ends in `.html`, do not open it directly** —\nrun `bash ../subagent-driven-development/scripts/task-brief-html --plan PLAN_FILE`\nand read the file it writes.",
      "required": true
    },
    {
      "find": "- Run this skill's `scripts/task-start PLAN_FILE N`. It prints the brief\n  path and BASE (the commit the task's range is cut from) in one call.",
      "replace": "- Run this skill's `scripts/task-start PLAN_FILE N`. It prints the brief\n  path and BASE (the commit the task's range is cut from) in one call.\n  **If PLAN_FILE ends in `.html`**, `task-start` cannot read it — run\n  `bash ../subagent-driven-development/scripts/task-brief-html PLAN_FILE N && git rev-parse HEAD`\n  instead: same brief path, same BASE, still one call. `task-done` works\n  unchanged for either format.",
      "required": true
    },
    {
      "find": "[Read plan once: docs/superpowers/plans/feature-plan.md; spec read]",
      "replace": "[Read plan body once: bash ../subagent-driven-development/scripts/task-brief-html --plan docs/superpowers/plans/feature-plan.html; spec read]",
      "required": true
    },
    {
      "find": "[Resolve workspace: sdd-workspace docs/superpowers/plans/feature-plan.md",
      "replace": "[Resolve workspace: sdd-workspace docs/superpowers/plans/feature-plan.html",
      "required": true
    },
    {
      "find": "[task-start plan 1 → brief read; BASE a1b2c3d]",
      "replace": "[task-brief-html plan 1 + rev-parse → brief read; BASE a1b2c3d]",
      "required": true
    },
    {
      "find": "[task-start plan 2 → brief read; BASE d4e5f6a]",
      "replace": "[task-brief-html plan 2 + rev-parse → brief read; BASE d4e5f6a]",
      "required": true
    }
  ],
  "append": "additions.md"
}
