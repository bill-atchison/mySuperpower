
## mySuperpower additions — HTML implementation notes

After the final whole-branch review, capture implementation notes as a **self-contained, styled HTML document** using the template at `skills/subagent-driven-development/templates/implementation-notes-template.html`. Save to `docs/mySuperpower/implementation-notes/YYYY-MM-DD-<feature>.html` and commit, then proceed to `my-superpower:finishing-a-development-branch`.
